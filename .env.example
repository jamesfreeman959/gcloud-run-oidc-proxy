# Cloud Run URL (required)
CLOUD_RUN_URL=https://your-service-xyz.a.run.app

# Path inside the container where the service account JSON will be mounted
GOOGLE_APPLICATION_CREDENTIALS=/etc/proxy/service-account.json

# Optional
HTTP_TIMEOUT_S=600
TOKEN_REFRESH_SKEW_S=60
